# GameWorld 项目开发日志

## 开发时间线

### 2026-03-23 功能更新

#### 1. 管理员后台
- 账户：mokyarcher
- 功能：用户管理、筹码操作

#### 2. 用户资料系统
- 修改昵称
- 头像选择（21个头像可选）

#### 3. 好友系统
- 搜索添加好友
- 申请/接受/拒绝流程
- 在线状态实时显示

#### 4. 在线状态追踪
- 实时显示玩家当前位置
- 位置类型：游戏大厅 / 德州扑克大厅 / 游戏中

#### 5. 邀请好友进房间
- 房主点击空座位打开邀请弹窗
- 显示在线好友列表
- 被邀请方收到实时弹窗通知
- 被邀请方可接受（跳转到房间）或拒绝

#### 6. 空座位样式优化（2026-03-23）
- 边框：淡蓝色虚线，带呼吸动画效果
- 背景：深蓝渐变，悬停时变亮
- 图标："+" 换成 "👥"（双人图标）
- 文字："点击邀请" 改成 "邀请好友"，淡蓝色
- 悬停效果：上浮、发光、放大
- 呼吸动画周期：4秒（舒缓节奏）

#### 7. 房间自动解散机制（2026-03-23）
- 玩家离开房间后，如果只剩人机且游戏未开始
- **立即解散房间**（无需等待）
- 清理数据库记录和房间资源

#### 8. 弹窗按钮位置调整（2026-03-23）
- 创建房间弹窗：**确认** 放左边，**取消** 放右边
- 离开房间弹窗：**确定** 放左边，**取消** 放右边

#### 9. 房间内点击头像添加好友（2026-03-23）
- 点击其他玩家头像弹出添加好友确认框
- 显示玩家头像和昵称
- 发送好友请求到后端

#### 10. 修复房间解散 Bug（2026-03-23）
- 修复：被邀请玩家离开后房间被意外解散的问题
- 调整：只剩房主一人时保留房间（不再自动解散）

#### 11. 房主身份转移（2026-03-23）
- 房主退出房间时，房主身份自动转移给最靠前的真人玩家
- 新房主收到系统通知
- 原房主重新进入房间时为普通玩家身份

#### 12. 修复座位分配 Bug（2026-03-23）
- 修复：老房主重新进入房间时座位显示不正确的问题
- 优化：座位号现在找第一个空位，而不是简单使用 players.length

#### 13. 修复掉线倒计时显示（2026-03-23）
- 修复：玩家掉线后其他人看不到倒计时的问题
- 新增：掉线玩家头像显示红色遮罩和倒计时
- 新增：掉线状态文字显示（"掉线 60s"、"掉线 59s"...）
- 新增：掉线玩家边框变红色并闪烁
- 修复：userId 类型不匹配导致无法找到掉线玩家的问题
- 修复：socketMap 中 userId 未统一存储为字符串的问题
- 修复：掉线遮罩层级问题，添加 z-index 确保显示在最上层
- 修复：后端缺少 `get-room-state` 事件处理，导致房间状态无法同步
- 优化：掉线倒计时位置从头像移到信息框上方（筹码和状态之间）
- 优化：game.html 中掉线标记改为倒计时显示（红色，头像下方）
- 移除：game.html 中的 ⚠ 掉线标记

#### 14. 修复掉线倒计时未显示问题（2026-03-23）
- **问题**：⚠ 掉线标记已隐藏，但红色倒计时未显示
- **原因**：`disconnect-countdown` 事件错误调用了 `updatePlayerTimer`（操作倒计时函数），且 `game-state` 更新时会覆盖 `disconnectCountdown` 值
- **修复**：
  - 修改 `disconnect-countdown` 事件处理：正确存储倒计时值到 `player.disconnectCountdown` 并调用 `renderPlayerSeats()`
  - 修改 `game-state` 事件处理：更新前保存现有的 `disconnectCountdown` 值，更新后恢复
  - 添加调试日志：在 `renderPlayerSeats` 中输出 `disconnectCountdown` 值便于调试
- **文件**：`frontend/games/poker/game.html`

#### 15. 新增房主删除房间功能（2026-03-23）
- **功能**：房主退出房间后回到大厅，可以在房间列表中删除自己创建的房间
- **实现**：
  - `lobby.html`：房间列表操作列宽度从 120px 调整为 180px，添加红色删除按钮（仅房主可见）
  - `lobby.html`：添加 `deleteRoom()` 函数，发送 `delete-room` 事件到后端
  - `lobby.html`：添加 `room-deleted` 和 `delete-room-error` 事件监听
  - `poker.socket.js`：
    - `get-rooms` 事件：返回房间列表时增加 `ownerId` 字段
    - 新增 `delete-room` 事件处理：验证房主身份，清理房间资源，从数据库删除记录，通知所有玩家
  - `room.html`：添加 `room-deleted-by-owner` 事件监听，房间被删除时弹出提示并跳转回大厅
- **优化**（2026-03-23）：删除房间后自动隐藏重连按钮
  - `lobby.html`：`room-deleted` 事件处理中清除 `activeGameInfo` 并隐藏重连按钮
- **文件**：
  - `frontend/games/poker/lobby.html`
  - `frontend/games/poker/room.html`
  - `backend/games/poker/poker.socket.js`

---

## 技术栈
- Node.js + Express
- Socket.io
- SQLite3
- 前端：HTML + CSS + JavaScript

## 开发端口
- 5555

## 启动方式
```bash
npm start
```
